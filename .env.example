# Copy this file to `.env` and override values as needed before starting the API server.

# Shared API key that clients (e.g. browser extension) must supply via the X-API-Key header.
WEBPENTEST_API_KEY=change-me

# Comma-separated list of origins allowed by CORS. Use * for development only.
API_ALLOWED_ORIGINS=http://localhost:3000,chrome-extension://<extension-id>

# Maximum number of concurrent scan workers the API will spawn.
API_MAX_WORKERS=2

# Optional log verbosity for the API server (DEBUG, INFO, WARNING, ERROR).
API_LOG_LEVEL=INFO

# Directory where generated scan reports will be stored.
REPORTS_DIR=reports

# Port used when running `uvicorn api_server:app` locally.
PORT=8000
